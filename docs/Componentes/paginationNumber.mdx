---
id: pagination-number
title: Pagination Number
sidebar_label: Pagination Number
custom_edit_url: null
---

import PaginationNumber from "../../src/components/PaginationNumber";

## Como utilizar?

Para utilizar o componente é necessário realizar a importação.

```tsx
import PaginationNumber from "../components/PaginationNumber";
```

Após realizar a importação adicione o componente no arquivo `.tsx`.

**Utilização básica**:

```tsx
<PaginationNumber count={6}></PaginationNumber>
```

<PaginationNumber count={6}></PaginationNumber>

## Propriedades

### Count:

A propriedade `count` define o número de elementos do componente. `count` é obrigatório.

```tsx
<Pagination count={10}></Pagination>
```

<Pagination count={10}></Pagination>
